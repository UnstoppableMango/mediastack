trigger:
- master

resources:
- repo: self

stages:
# - stage: Build
#   pool: Linux
#   steps:
#   - checkout: self

- stage: Deploy
  jobs:
  - deployment: Deploy
    environment: Prod
    strategy:
      runOnce:
        deploy:
          pool: Local
          steps:
          - bash: 'echo "Test Step"'
